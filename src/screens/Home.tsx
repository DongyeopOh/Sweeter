import React from "react";
import { Article } from "../commons/styled";
import Template from "../components/Template";

const Home: React.FunctionComponent = () => {
  return (
    <Template>
      <Article />
    </Template>
  );
};

export default Home;
